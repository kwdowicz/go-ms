syntax = "proto3";

package commns;

option go_package = "github.com/kwdowicz/go-ms/comms";

message Msg {
    string content = 1;
}

message Ack {
    string message = 1;
}

service PersonService {
    rpc Post (Msg) returns (Ack);
}