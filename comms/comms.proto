syntax = "proto3";

package commns;

option go_package = ".";

message Msg {
    string content = 1;
    Tag tag = 2;
}

message Ack {
    string message = 1;
}

message Tag {
    string name = 1;
}

service MsgService {
    rpc Post (Msg) returns (Ack);
    rpc Sub (Tag) returns (Ack);
}